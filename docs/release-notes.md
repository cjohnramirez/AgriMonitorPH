# Release Notes

## [v0.5-scm] - April 23, 2026
### Added
* Established Git branching strategy (`feature/`, `bugfix/`, `hotfix/`).
* Created `.github/CODEOWNERS` for PR review delegation.
* Added `src/settings.ts` module for environment variables.
* Simulated and successfully resolved a git merge conflict in `README.md`.